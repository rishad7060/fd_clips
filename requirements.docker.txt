# Slim requirements for the CPU-path Docker image.
# Heavy GPU deps (whisperx, pyannote, torch, torchvision) are intentionally
# excluded — the free CPU path uses Groq for transcription and MediaPipe for
# face detection instead. Add a separate gpu.requirements.txt for RunPod builds.

# Ingest
yt-dlp>=2024.0.0
ffmpeg-python>=0.2.0

# Config / env
python-dotenv>=1.0.0

# Scoring LLM clients (free-tier paths)
openai>=1.30.0
google-genai>=0.3.0
groq>=1.4.0

# CPU face detection for smart crop
mediapipe>=0.10.0
# headless variant — no GUI/display required in containers
opencv-python-headless>=4.9.0

# Scene detection (used by reframe.py)
scenedetect>=0.6.3

# Numerics
numpy>=1.26.0

# Utilities
pydantic>=2.6.0
rich>=13.7.0

# Email delivery of finished clips
resend>=2.0.0
