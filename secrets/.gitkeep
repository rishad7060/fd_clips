# This dir holds yt-dlp cookies.txt (the YouTube bot-check fix), which is
# gitignored and NEVER committed. This .gitkeep keeps the empty dir in git so
# the docker-compose bind mount (./secrets:/secrets:ro) has a source on a fresh
# server checkout. See YOUTUBE_COOKIES_RUNBOOK.md.
